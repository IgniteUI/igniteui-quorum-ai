You are voting on the execution of a step of the task:
<task>
{TASK}
</task>

<rules>
{RULES}
</rules>

The task is to execute the current step based on the provided task and return the output state.
Be VERY strict and precise in your evaluation. Ensure that the task and current step are fully satisfied by the resulting state.
Reject any changes which are not specifically required by the task or current step.
The resulting state must only include changes that are explicitly required by the current step.
Each step modifies exactly ONE thing — one field, one value, one concept. If a single step changes more than one thing without the Step explicitly allowing it, vote "No".
BATCH CLARIFICATION: Multiple steps may be executed together in one batch. When a batch contains N steps, the resulting state is allowed to have up to N changes — one per step. Evaluate atomicity per step, not for the batch as a whole. For example, if 3 steps each target a different property, 3 properties changing in the resulting state is correct and expected.
EXCEPTION — FINAL ASSEMBLY: If the step is final output assembly/generation (assembling, composing, or formatting the final output), it is permitted to touch multiple fields. Vote "Yes" as long as all previously determined values are preserved exactly and the output matches the required format. Vote "No" only if the final assembly alters, drops, or invents values.
Ensure that ALL rules are followed!

Vote with a simple "Yes" or "No" verbatim on whether this is an appropriate solution based on the task and current state.
Evaluate whether the step is indeed completed correctly and vote "Yes" or "No" accordingly.
Vote "No" if there is no change to the state unless explicitly specified that changes are optional or the change was already present in the state.
When voting "No" you can provide a short <= 100 character plaintext reason on a new line with no additional formatting.

The steps to execute are:
<current_steps>
{STEP}
</current_steps>

The current state is:
<current_state>
{CURRENT_STATE}
</current_state>

The suggested new state is:
<new_state>
{STATE}
</new_state>